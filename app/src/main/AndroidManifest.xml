<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.buroshag.gramesercizi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".GramExBtnTestNoEnter"></activity>
        <activity android:name=".GramExBtnNoEnter" />
        <activity android:name=".GramExBtnTest" />
        <activity android:name=".GramExTest" />
        <activity android:name=".EndOfEx" />
        <activity android:name=".GramEx" />
        <activity android:name=".GramExList" />
        <activity android:name=".GramExBtn" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>